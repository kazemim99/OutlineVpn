# HTTP - redirect all requests to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name iransshvpn.com www.iransshvpn.com;

    # Let's Encrypt challenge location
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP requests to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS - proxy requests to the backend
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name iransshvpn.com www.iransshvpn.com;

    # SSL certificates (will be created by certbot)
    ssl_certificate /etc/letsencrypt/live/iransshvpn.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iransshvpn.com/privkey.pem;

    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Client upload size
    client_max_body_size 100M;

    # Proxy to the backend API
    location /api/ {
        proxy_pass http://v2ray-api:5000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # Serve static files (Vue.js frontend)
    location / {
        proxy_pass http://v2ray-api:5000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Logs
    access_log /var/log/nginx/iransshvpn_access.log;
    error_log /var/log/nginx/iransshvpn_error.log;
}
